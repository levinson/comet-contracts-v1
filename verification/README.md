# Machine-checked c_pow lemmas

This Lean project machine-checks the baseline `c_pow` recurrence-error budget, geometric-tail simplification, operating-band convergence, exact-input and exact-output swap composition, successful source-shaped execution bridges for initialization, both swap directions, all four single-sided liquidity directions, and both proportional liquidity directions, spot-price getter error and monotonicity bounds, configured limits, and application-specific signed-`I256` magnitude and denominator bounds.

The checked-in Lean constants are generated from the production Rust constants,
so the proof cannot silently keep using old pool ratios, fees, weights,
precision, exponent, or iteration limits. From the repository root, check that
the generated module is current with:

```sh
rustc --edition=2021 verification/generate_constants.rs -o /tmp/generate-cpow-constants
/tmp/generate-cpow-constants --check verification/CometCPow/GeneratedConstants.lean
```

To regenerate it after changing a source constant, replace the `--check` line
with:

```sh
/tmp/generate-cpow-constants > verification/CometCPow/GeneratedConstants.lean
```

Then run the proofs with:

```sh
cd verification
lake build
```

Elan caches the pinned Lean compiler outside the repository. To populate that persistent cache explicitly before the first build, run `elan toolchain install leanprover/lean4:v4.19.0`; later `lake` commands reuse `~/.elan/toolchains/leanprover--lean4---v4.19.0`.

The checked-in `lake-manifest.json` pins the complete dependency graph, so routine local builds should not run a bare `lake update`; that command refreshes every Git dependency and can fetch substantial Mathlib history. After intentionally changing one dependency pin in `lakefile.toml`, run `lake update <package-name>` once, commit the resulting manifest, and return to `lake build` for normal development.

The project pins Lean and mathlib to version 4.19.0. The
`c_pow formal verification` workflow first rejects a stale generated constants
module, then runs the build for every relevant pull-request change and rejects
proof sources containing `sorry`, `admit`, or custom `axiom` declarations. It
also audits the compiled `CometCPow` namespace transitively, allowing only
Lean's standard `propext`, `Classical.choice`, and `Quot.sound` foundations.

The verification project is development-only. It is not a dependency of any
Rust crate and does not alter the contract's instructions, memory use, or WASM
size.

## Checked claims

| Proof obligation | Lean theorem |
| --- | --- |
| Mathematical floor/ceiling errors are below one raw unit | `floor_isFloor`, `ceil_isCeil`, `IsFloor.abs_error_lt_one`, `IsCeil.abs_error_lt_one` |
| One recurrence step adds less than three raw units | `recurrence_increment_lt_three` |
| The three exact floors in a concrete baseline recurrence update imply the raw one-step error inequality | `three_floor_recurrence_step_error` |
| On the below-one path, flooring a product before division by a positive integer is equivalent to a single floor at the combined divisor, and coefficient rounding is favorable to the pool-adverse direction | `floor_floor_div_nat`, `below_one_recurrence_step_signed_error` |
| The raw recurrence inequality implies the `3k - 2` term budget through the stopping index actually reached by production | `recurrence_error_budget_until` |
| The summed scalar budgets equal `(3N² - N) / 2` | `sum_error_budget` |
| The per-step recurrence inequality and first floor refinement imply the complete finite-partial-sum error budget through the executed stopping index | `partial_sum_error_lt_sum_error_budget`, `recurrence_implies_partial_sum_error_budget_until` |
| `q / (1 - q) <= 1` when `q <= 1/2` | `geometric_tail_le_current` |
| The fractional binomial recurrence implies the exact-term bound `abs(Tₙ) <= S qⁿ` | `fractional_binomial_factor_le_one`, `fractional_binomial_step_contracts`, `fractional_binomial_terms_geometric_bound` |
| Taylor's theorem identifies the exact generalized-binomial terms and proves every finite below-one partial sum upper-bounds real exponentiation for fractional exponents in `[0,1]` | `iteratedDerivWithin_one_sub_rpow`, `bone_mul_taylorWithinEval_eq_exactInputBinomialSum`, `exact_input_binomial_partial_upper` |
| The below-one generalized-binomial series sums to real exponentiation, its omitted negative tail is bounded by the last retained term times `(1 - base) / base`, and doubling that term makes the result a true lower bound when `base >= 0.5` | `exact_input_binomial_hasSum`, `exact_input_binomial_partial_tail_le`, `exact_input_binomial_doubled_last_partial_lower` |
| Taylor's theorem identifies the above-one generalized-binomial terms and proves every odd partial sum upper-bounds real exponentiation for fractional exponents in `[0,1]` | `iteratedDerivWithin_one_add_rpow`, `bone_mul_taylorWithinEval_eq_exactOutputBinomialSum`, `exact_output_binomial_odd_partial_upper` |
| Production input/output ratios, fees, and weights place the continuous configured anchors in the `[0.5, 1.6]` operating envelope | Generated `MAX_IN_RATIO`, `MAX_OUT_RATIO`, `MAX_FEE`, and `MAX_WEIGHT`; the `configured_*_margin` and `configured_*_base_*` lemmas in `PoolConfig.lean` |
| Every base in `[0.5, 1.6]` satisfies `abs(x) <= 3/5` | `operating_base_implies_abs_x_le_three_fifths` |
| The computed term reaches production precision by iteration 46 throughout `[0.5, 1.6]`, before the configured cap | `pool_operating_band_numeric_margin`, `pool_operating_band_iteration_within_cap`, `pool_operating_band_converges_by_iteration_46`, `pool_operating_base_converges_by_iteration_46` |
| On the baseline exact-input path, the signed term budgets sum to `(3N² + N + 3) / 8` for `N >= 2`, and multi-term pool-adverse approximation error is below `17/800 = 2.125%` of the minimum fee's spot-normalized value; the one-term correction has no adverse error | `exact_input_signed_term_error_lt_budget`, `exact_input_signed_accumulated_error_value`, `baseline_exact_input_multiterm_adverse_error_lt_precise_fee_share`, `first_term_upper_has_no_adverse_error` |
| The exact-input caller's input floor, base ceiling, exponent floor, integer/fractional `c_pow` composition, output multiplication floor, and token downscale compose into the same 2.125% bound for integer-only, unit-base, corrected first-term, and multi-term control-flow cases | `baseline_swap_exact_amount_in_integer_adverse_error_lt_precise_fee_share`, `baseline_exact_input_cpow_unit_base_has_no_adverse_error`, `baseline_swap_exact_amount_in_first_term_adverse_error_lt_precise_fee_share`, `baseline_swap_exact_amount_in_multiterm_adverse_error_lt_precise_fee_share` |
| A successful executable source-shaped `calc_token_out_given_token_in` result reconstructs its checked i128/I256 arithmetic, preserves the exact STROOP-to-BONE exponent split, proves the configured integer exponent is at most 9 so Rust's `u32` conversion is lossless, evaluates only recurrence prefixes actually reached by the 50-iteration loop, covers every `c_pow` control-flow branch, and directly satisfies the same 2.125% bound under the public positive-input, ratio, fee, and weight invariants | `scaledExponentSourceSplit`, `calcTokenOutGivenTokenInExecution`, `successfulExactInputMathRun_of_execution`, `successfulExactInputMathRun_refines`, `successfulExactInputMathRun_adverse_error_lt_precise_fee_share`, `calc_token_out_given_token_in_execution_adverse_error_lt_precise_fee_share` |
| The exact-output caller's base ceiling, exponent ceiling, above-one integer/fractional `c_pow` composition, fee division ceiling, and token downscale ceiling compose into a `4501/100000 = 4.501%` adjusted minimum-fee bound for integer-only, corrected first-term, second-term, and later odd/even control-flow cases | `baseline_swap_exact_amount_out_integer_adverse_error_lt_precise_fee_share`, `baseline_swap_exact_amount_out_first_term_adverse_error_lt_precise_fee_share`, `baseline_swap_exact_amount_out_second_term_adverse_error_lt_precise_fee_share`, `baseline_swap_exact_amount_out_later_adverse_error_lt_precise_fee_share` |
| A successful executable source-shaped `calc_token_in_given_token_out` result reconstructs its checked i128/I256 arithmetic, exact exponent split, upper-rounded integer-power trace, executed recurrence prefixes, and every exact-output `c_pow` correction branch, and directly satisfies the same 4.501% adjusted minimum-fee bound under the public positive-output, ratio, fee, and weight invariants | `calcTokenInGivenTokenOutExecution`, `successfulExactOutputMathRun_of_execution`, `successfulExactOutputMathRun_refines`, `successfulExactOutputMathRun_adverse_error_lt_precise_fee_share`, `calc_token_in_given_token_out_execution_adverse_error_lt_precise_fee_share` |
| The exact-LP-input single-sided withdrawal's supply-ratio ceiling, reciprocal-weight exponent floor, below-one `c_pow` composition, new-balance ceiling, fee multiplication floor, token downscale floor, and successful `MAX_OUT_RATIO` check compose into a `3001/100000 = 3.001%` weighted minimum-fee bound for every production control-flow case | `baseline_single_sided_withdrawal_integer_adverse_error_lt_precise_fee_share`, `baseline_single_sided_withdrawal_unit_base_adverse_error_lt_precise_fee_share`, `baseline_single_sided_withdrawal_first_term_adverse_error_lt_precise_fee_share`, `baseline_single_sided_withdrawal_multiterm_adverse_error_lt_precise_fee_share`, `successful_single_sided_withdrawal_low_base_has_no_adverse_error` |
| A successful executable source-shaped `calc_token_withdrawal_amount_given_lp_token_amount` result reconstructs its checked i128/I256 scaling, supply-ratio ceiling, reciprocal-weight floor, arbitrary BONE-precision exponent split, upper-rounded integer-power and recurrence traces, new-balance ceiling, weighted-fee and token downscale floors, and both public balance-cap guards, covers every production correction branch using only executed loop prefixes, and directly satisfies the same 3.001% weighted minimum-fee bound | `calcTokenWithdrawalAmountGivenLpTokenAmountExecution`, `calcTokenWithdrawalAmountGivenLpTokenAmountExecution_refines`, `calc_token_withdrawal_amount_given_lp_token_amount_execution_adverse_error_lt_precise_fee_share` |
| The exact-LP-output single-sided deposit's supply-ratio and reciprocal-exponent ceilings, above-one `c_pow` composition, new-balance ceiling, weighted-fee division ceiling, token downscale ceiling, and successful `MAX_IN_RATIO` check compose into a `1107/100000 = 1.107%` adjusted weighted minimum-fee bound for every production control-flow case | `baseline_single_sided_deposit_integer_adverse_error_lt_precise_fee_share`, `baseline_single_sided_deposit_first_term_adverse_error_lt_precise_fee_share`, `baseline_single_sided_deposit_second_term_adverse_error_lt_precise_fee_share`, `baseline_single_sided_deposit_later_adverse_error_lt_precise_fee_share` |
| A successful executable source-shaped `calc_token_deposits_in_given_lp_token_amount` result reconstructs its checked i128/I256 scaling, supply-ratio and reciprocal-weight ceilings, arbitrary BONE-precision exponent split, upper-rounded integer-power and recurrence traces, new-balance and weighted-fee division ceilings, token downscale ceiling, and successful public `MAX_IN_RATIO` floor/guard, covers every production correction branch using only executed loop prefixes, and directly satisfies the same 1.107% adjusted weighted minimum-fee bound | `singleSidedDepositCPowExecution`, `calcTokenDepositsInGivenLpTokenAmountExecution`, `calcTokenDepositsInGivenLpTokenAmountExecution_refines`, `calc_token_deposits_in_given_lp_token_amount_execution_adverse_error_lt_precise_fee_share` |
| The exact-token-input single-sided deposit's fee-adjusted input floor, balance-ratio floor, direct-weight above-one `c_pow` composition, new-supply floor, and LP-token downscale floor compose into a `1223/100000 = 1.223%` weighted minimum-fee bound for unit-base, zero-first-term, corrected-first-term, second-term, and later control-flow cases | `baseline_single_sided_token_deposit_unit_base_adverse_error_lt_precise_fee_share`, `baseline_single_sided_token_deposit_zero_first_term_adverse_error_lt_precise_fee_share`, `baseline_single_sided_token_deposit_first_term_adverse_error_lt_precise_fee_share`, `baseline_single_sided_token_deposit_second_term_adverse_error_lt_precise_fee_share`, `baseline_single_sided_token_deposit_later_adverse_error_lt_precise_fee_share` |
| A successful executable source-shaped `calc_lp_token_amount_given_token_deposits_in` result reconstructs its checked i128/I256 scaling, exact weighted-fee multiplier, adjusted-input and balance-ratio floors, direct-weight above-one `c_pow` trace, new-supply floor, subtraction, and LP-token downscale, then delegates every correction branch to the corresponding complete operation theorem and satisfies the same 1.223% weighted minimum-fee bound under the public positive-input, ratio, fee, and weight invariants | `calcLpTokenAmountGivenTokenDepositsInExecution`, `calcLpTokenAmountGivenTokenDepositsInExecution_refines`, `lowerAboveApproxExecution`, `lowerCPowExecution`, `calc_lp_token_amount_given_token_deposits_in_execution_adverse_error_lt_precise_fee_share` |
| The exact-token-output single-sided withdrawal's maximum-output guard, fee-division ceiling, remaining-balance-ratio floor, direct-weight below-one `c_pow` composition, new-supply floor, and LP-token downscale ceiling compose into a `4751/100000 = 4.751%` adjusted weighted minimum-fee bound for every first-, second-, and later-term case reached by a successful positive-base call | `baseline_single_sided_token_withdrawal_first_term_adverse_error_lt_precise_fee_share`, `baseline_single_sided_token_withdrawal_second_term_adverse_error_lt_precise_fee_share`, `baseline_single_sided_token_withdrawal_later_adverse_error_lt_precise_fee_share` |
| A successful executable source-shaped `calc_lp_token_amount_given_token_withdrawal_amount` result reconstructs its checked i128/I256 scaling, exact weighted-fee multiplier, before-fee ceiling, remaining-balance floor, direct-weight below-one `c_pow` trace, new-supply floor, subtraction, and LP-token downscale ceiling, covers every correction and low-base branch using only executed loop prefixes, and directly satisfies the same 4.751% adjusted weighted minimum-fee bound under the public positive-output, ratio, fee, and weight invariants | `calcLpTokenAmountGivenTokenWithdrawalAmountExecution`, `calcLpTokenAmountGivenTokenWithdrawalAmountExecution_refines`, `lowerBelowApproxExecution`, `lowerBelowCPowExecution`, `calc_lp_token_amount_given_token_withdrawal_amount_execution_adverse_error_lt_precise_fee_share` |
| Below-one integer-power composition preserves the adverse-error fee comparison | `below_one_upper_composition_preserves_adverse_bound` |
| An exponentiation-by-squaring trace whose multiplications use exact ceiling refinements upper-bounds the natural power computed by `c_powi` | `UpperCPowiLoop.upper_bound`, `UpperCPowiTrace.upper_bound`, `normalized_mul_ceil_upper` |
| Retaining the exact `1/2` contraction in the second binomial term gives a reusable `3.9601%` recurrence rate and `3.961%` selected fee share throughout the full three-fifths operating band | `fractional_binomial_second_term_contracts`, `fractional_binomial_terms_from_second_bound`, `accumulated_error_lt_three_fifths_later_fee_rate`, `accumulated_error_lt_three_fifths_weighted_later_fee_rate`, `baseline_operating_band_later_adverse_error_lt_precise_fee_share` |
| For the exact-LP-input withdrawal, retaining the actual `1/2` contraction makes every later finite recurrence budget less than `3.0001%` of the minimum-fee first-term scale; the signed second stop is below `9/4` raw units, and all branches compose below 3.001% | `single_sided_withdrawal_accumulated_error_lt_later_fee_rate`, `single_sided_withdrawal_second_stop_signed_error_lt_nine_fourths`, `single_sided_withdrawal_precise_fee_value_dominates_first_term`, `baseline_single_sided_withdrawal_cpow_multiterm_adverse_error_lt_precise_fee_share` |
| For the exact-token-output withdrawal, the shared augmented half-band theorem makes every later recurrence budget less than `4.7501%` of the weighted minimum-fee first-term scale; the second stop is much smaller, and all branches compose below 4.751% | `augmented_error_lt_half_later_fee_rate`, `baseline_single_sided_token_withdrawal_cpow_second_term_adverse_error_lt_precise_fee_share`, `baseline_single_sided_token_withdrawal_cpow_later_adverse_error_lt_precise_fee_share` |
| For exact-output swaps, retaining the configured `0.500001` base-displacement ceiling makes the finite recurrence budget less than `3.0001%` of the minimum-fee first-term scale; the `1.500001` ratio-relative displacement and exponent ceiling compose every branch below 4.501% | `configured_exact_output_computed_displacement_lt_precise`, `accumulated_error_lt_exact_output_precise_fractional_fee_of_later_terms`, `configured_continued_exact_output_displacement_le_precise`, `exact_output_weighted_first_term_fee_scale_lt_precise_share`, `baseline_exact_output_cpow_adverse_error_lt_precise_fee_share` |
| At the generic exact-output recurrence layer, the second-iteration adjustment removes the negative second term, leaving a sub-unit first-term floor error; continuation and the coarse `8/5` displacement bridge bound that error below `2/125 = 1.6%` of the minimum fee | `exact_output_second_iteration_fee_value_dominates_first_term`, `baseline_exact_output_second_iteration_adverse_error_lt_precise_fee_share`, `baseline_configured_exact_output_second_iteration_adverse_error_lt_precise_fee_share` |
| Exact-output continuation gives the nominal-ratio lower bound needed to absorb both base and exponent ceiling units; the shared displacement lemma gives `1.52 * nominal_ratio`, while the swap-specific configured refinement gives `1.500001 * nominal_ratio` | `continued_exact_output_forces_nominal_ratio_lower_sharp`, `configured_continued_exact_output_displacement_le`, `configured_continued_exact_output_displacement_le_precise`, `exact_output_fractional_exponent_le_adjusted_ideal` |
| At the abstract recurrence layer, the reciprocal exponent's fractional part is bounded by the single-sided weighted-fee exponent, and the conditional later reciprocal single-sided fractional result remains below 3.961% | `reciprocal_fractional_part_le_fee_exponent`, `single_sided_fee_value_dominates_reciprocal_fractional_term_precise`, `baseline_reciprocal_single_sided_later_adverse_error_lt_precise_fee_share` |
| At the abstract recurrence layer, the exact `(1 - weight)` second-term factor makes the conditional later direct single-sided adverse error smaller than 3.961% of the weighted minimum fee | `fractional_binomial_second_term_weighted_bound`, `accumulated_error_lt_three_fifths_weighted_later_fee_rate`, `baseline_direct_single_sided_later_adverse_error_lt_precise_fee_share` |
| For the exact-LP-output deposit, splitting at a `1/200` nominal mint ratio combines a one-percent small-ratio fractional budget with sharper base and whole-power bounds, while the large-ratio branch uses the universal 3151-unit cap; every branch composes below 1.107% | `accumulated_error_lt_small_ratio_fee_rate_of_later_terms`, `single_sided_deposit_small_whole_power_lt`, `single_sided_deposit_whole_power_lt_1718_div_25`, `single_sided_deposit_small_ratio_fee_scale_lt_precise_share`, `baseline_single_sided_deposit_cpow_adverse_error_lt_precise_fee_share` |
| For the exact-token-input deposit, the public `MAX_IN_RATIO` contraction and the even Taylor degree retained by every later round-down stop sharpen that generic direct-path estimate to 1.223%; the first-stop omitted second term satisfies the same bound | `single_sided_token_deposit_later_accumulated_error_lt_precise_fee_rate`, `single_sided_token_deposit_precise_fee_value_dominates_weighted_first_term`, `single_sided_token_deposit_first_stop_second_term_lt_precise_fee_share`, `baseline_single_sided_token_deposit_cpow_later_adverse_error_lt_precise_fee_share` |
| Non-negative ratio and amount bounds compose algebraically into pool-favoring proportional join/exit bounds | `proportional_join_rounding_is_pool_favoring`, `proportional_exit_rounding_is_pool_favoring` |
| The proportional `join_pool` and `exit_pool` ratio division, token multiplication, and final token downscale compose per token so a join cannot undercharge and an exit cannot overpay | `join_pool_per_token_cannot_undercharge`, `exit_pool_per_token_cannot_overpay` |
| A successful executable source-shaped `join_pool` arithmetic result reconstructs the shared checked join-ratio ceiling, applies it in source order to every modeled token record, reconstructs each checked multiplication and downscale ceiling, enforces the positive-output, positive user-maximum, and maximum-input guards, preserves the checked balance-addition equation, and proves every returned token amount is no less than its ideal proportional share | `joinPoolRatioExecution`, `joinPoolTokenExecution`, `joinPoolTokensExecution`, `joinPoolExecution`, `joinPoolExecution_every_token_cannot_undercharge` |
| A successful executable source-shaped `exit_pool` arithmetic result reconstructs the shared checked exit-ratio floor, applies it in source order to every modeled token record, reconstructs each checked multiplication and downscale floor, enforces the positive-output, user-minimum, and available-balance guards, preserves the balance-update equation, and proves every returned token amount is no greater than its ideal proportional share | `exitPoolRatioExecution`, `exitPoolTokenExecution`, `exitPoolTokensExecution`, `exitPoolExecution`, `exitPoolExecution_every_token_cannot_overpay` |
| Positive active-pool balances and configured weights make every `get_spot_price` divisor positive | `weighted_balance_one_le`, `get_spot_price_divisors_positive` |
| The scaled ideal is the usual weighted spot-price formula, and the complete nested-floor calculation stays inside an explicit two-sided error envelope around it | `ideal_spot_price_eq_weighted_ratio`, `spot_price_floor_chain_bounds`, `get_spot_price_from_fixed_point_refinements` |
| The zero-fee final floor is an identity, a valid fee cannot lower the returned price, and the result is monotone in the fee | `get_spot_price_sans_fee_returns_ratio`, `get_spot_price_not_below_sans_fee`, `spot_price_is_monotone_in_fee` |
| Increasing the input balance and decreasing the output balance cannot reduce the computed post-swap spot price | `spot_price_does_not_decrease_after_swap_balance_update` |
| The executable source-shaped `calc_spot_price` model turns any successful return into the four production `FixedPoint for i128` results and directly establishes representability and the complete spot-price error envelope without caller-supplied rounding premises | `calcSpotPriceExecution`, `successfulSpotPriceRun_of_execution`, `i128_fixed_div_floor_success_refines`, `SuccessfulSpotPriceRun.refinements`, `calc_spot_price_execution_error_bounds` |
| Successful source-shaped spot-price executions additionally satisfy the zero-fee identity, fee monotonicity, and post-swap direction | `SuccessfulSpotPriceRun.sans_fee_returns_ratio`, `successful_get_spot_price_not_below_sans_fee`, `successful_spot_price_is_monotone_in_fee`, `successful_spot_price_does_not_decrease_after_swap_balance_update` |
| Non-negative upper bounds compose under multiplication | `mul_upper_bound` |
| The production Rust precision, exponent, scale, pool-configuration, initial-mint, minimum-balance, and iteration constants discharge the proof premises | Generated constants in `GeneratedConstants.lean`; `configured_exponent_limit`, `term_error_at_iteration_cap`, `real_scale_exceeds_term_error_cap`, `sum_error_at_iteration_cap` |
| A successful executable source-shaped `init` result enforces the uninitialized, token-count, vector-length, fee, duplicate-token, weight, balance, decimal, checked total-weight, and exact-total guards; preserves token, weight, balance, queried-decimal, and index alignment in the returned records; proves every derived scalar is positive and at most `BONE`; and establishes that the configured initial LP mint amount is positive | `initializationInputs`, `initializationRecordsExecution`, `initializationExecution`, `initializationRecordsExecution_success`, `initializationExecution_establishes_invariants` |
| The final conservative raw product and every recurrence denominator fit signed 256-bit arithmetic | `final_raw_product_fits_i256`, `cpow_raw_magnitude_bound_fits_i256`, `cpow_denominator_bound_fits_i256`, `cpow_denominator_fits_i256` |

## Minimum-fee comparison

For the below-one, round-up fractional path used by an exact-input swap, a one-term result receives the baseline's one-unit upward correction and therefore has no adverse approximation error. If the loop reaches an iteration `N >= 2`, the preceding computed term was greater than `CPOW_PRECISION`. The configured maximum input ratio and minimum fee sharpen the exact-input contraction factor to strictly less than `1/4`, so the `3k - 2` recurrence-error bound implies

```text
CPOW_PRECISION < abs(T_1) * (1/4)^(N - 2) + (3(N - 1) - 2).
```

The absolute `3k - 2` budget remains useful for convergence, but it overcounts pool-adverse error. On the below-one path every computed recurrence term is non-positive. The coefficient floor therefore contributes only in the favorable direction. In addition, `floor(floor(y) / k) = floor(y / k)` for every positive integer `k`, so the multiplication and term-index division contribute one joint floor unit rather than two independent units. Lean derives signed per-term ceilings of `1` for the first term, `9/8` for the second term, and `(3k - 1) / 4` for every later term `k`. Their accumulated budget is `(3N² + N + 3) / 8` for `N >= 2`.

For every `2 <= N <= 46`, Lean checks that the same continuation threshold forces this signed accumulated budget below `(17 / 800) * (MIN_FEE / STROOP) * abs(T_1)`. At `N = 2`, where the finite table reaches its maximum fee share, the budget is `17/8` raw units and the sign-specific first-term floor property proves `CPOW_PRECISION < abs(T_1)`, so the corresponding minimum-fee scale exceeds 100 raw units. A Taylor-remainder proof derives the generalized-binomial recurrence from the derivatives of `(1 - q)^a` and proves every finite partial sum is an upper bound on the exact below-one power, so the omitted negative tail cannot increase adverse error. Finally, `abs(T_1) = BONE * fractional_exponent * base_displacement`, while the minimum fee's spot-normalized value is `MIN_FEE_RATE * BONE * full_exponent * nominal_input_ratio`; the fractional exponent does not exceed the full exponent, and the computed base displacement does not exceed the nominal input ratio. This proves the adverse fractional approximation error is strictly below `17/800 = 2.125%` of the minimum fee's continuous spot-normalized output value. The below-one integer factor is at most one and cannot amplify the comparison.

The operation-level theorems instantiate the exact-input caller's fee-adjusted input floor, normalized base ceiling, weight-ratio exponent floor, below-one integer/fractional composition, final output multiplication floor, and token downscale floor. They cover an integer exponent, the degenerate unit-base fractional result for which the implementation intentionally skips its one-unit correction, a non-unit fractional path that stops after its first term, and a fractional path that reaches a later term. In the multi-term case, the same concrete floor trace supplies both the absolute recurrence budget needed for convergence and the signed recurrence budget needed for the sharper adverse-error comparison. The iteration-46 convergence theorem is then applied to that trace, so `N <= 46` follows from the loop continuation condition instead of appearing as an independent premise. `ExactInputSwapSource.lean` evaluates the bounded loop and every checked outer arithmetic operation as an executable `Option` model. Its `c_pow` bridge consumes the caller's actual BONE-scaled exponent, proves that scaling preserves the integer and remainder components, and models Rust's `as u32` conversion modulo `2^32`; the configured `MIN_WEIGHT` and `MAX_WEIGHT` bounds limit the integer part to 9, making that conversion exact. A successful result supplies the trace and floor/ceiling premises automatically; in particular, its recurrence proof uses only prefixes the loop actually evaluated and does not postulate successful future iterations after an early stop. The former source-shaped, operation-specific, and coarse 5%/10% corollaries are superseded by the strictly stronger 2.125% theorems and have been removed.

This is a one-sided safety result, not a bound on absolute approximation error. Conservative error may exceed the stated percentage without harming the pool, particularly for very small one-term inputs. The final token-unit floors are included directionally because they can only decrease the user-visible exact-input output; the fee denominator remains the minimum fee's continuous spot-normalized value.

For exact output, Taylor's theorem proves that every odd partial of `(1 + q)^a` is an upper bound. If the approximation stops on an odd term, the baseline retains that odd partial. If it stops on an even negative term, the round-up correction removes that term and leaves the preceding odd partial. The first-term correction is conservative; at the second stop, removing the negative term leaves only the first term's sub-unit floor error. Continuation makes the first term exceed `CPOW_PRECISION`, so one percent of its minimum-fee scale exceeds that remaining unit; the generic `8/5` displacement bridge then gives a strict 1.6% fee-relative bound. Later stops use the same concrete three-floor recurrence budget and derive `N <= 46` from the loop trace.

Above-one integer powers require an additional composition argument because they can amplify fractional error. The proof derives `integer_part <= 9` from the configured weight ratio and its ceiling. Below nominal ratio `1e-5`, the rounded base is below `1.02` and its whole power is below two, so the weighted fractional term remains bounded by the full ideal exponent after accounting for the exponent-ceiling unit. At larger ratios, the whole power is below 64 and the universal `3151`-unit recurrence budget is directly smaller than 4.501% of the ideal minimum-fee scale. The precise continuation result proves `base - 1 <= 1.500001 * nominal_ratio`, leaving strict margin for the exponent ceiling.

The exact-output entry theorems then compose the base and exponent ceilings, upper-rounded `c_powi`, fractional approximation, final `c_pow` ceiling, input multiplication ceiling, actual fee-denominator ceiling, and token downscale ceiling. The precise proof retains the configured `0.500001` base-displacement ceiling and checks the finite recurrence budget through iteration 46 below `3.0001%` of the minimum-fee first-term scale. The ratio-relative displacement is at most `1.500001`, and the exponent ceiling contributes a factor below `1.000001`; their product gives a strict `4501/100000 = 4.501%` bound on ideal required input minus returned required input relative to `MIN_FEE_RATE * input_balance * ideal_exponent * nominal_output_ratio / (1 - actual_fee_rate)`. This is the minimum fee's continuous spot-normalized input value adjusted by the same fee denominator used by the caller. `ExactOutputSwapSource.lean` evaluates that arithmetic as an executable `Option` model, derives every refinement from a successful return, and restricts recurrence premises to the prefixes the production loop actually reached.

## Additional operation paths

The exact-output fractional path has a larger base displacement than exact input. `OperationFeeBound.lean` retains the second binomial term's division by two for shared operating-band estimates and isolates the second iteration's one-percent first-term rate; `BinomialAboveUpper.lean` and `ExactOutputSwap.lean` discharge the analytic direction, concrete recurrence, whole-power, and caller-composition obligations. The older `8/5` displacement bridge supports the standalone 1.6% second-iteration comparison, while the complete swap theorem uses the sharper `38/25` bridge to absorb the exponent ceiling and above-one whole-power composition.

Single-sided operations charge the weighted fee `(1 - weight) * swap_fee`. For the exact-LP-input withdrawal's reciprocal exponent, Lean proves that the fractional part of `1 / weight` is at most `(1 - weight) / weight`, so the smaller fee and reciprocal exponent compensate algebraically. Its operation-level theorems cover the integer-only, unit-base, corrected first-term, and multi-term `c_pow` cases and include the supply-ratio and exponent rounding, upper-rounded whole and fractional power composition, new-token-balance ceiling, fee multiplication floor, token downscale floor, and successful maximum-output guard. In the `[0.5, 1]` operating band, the second-stop proof uses a signed error below `9/4` raw units, while every later stop retains the exact `1/2` contraction and checks the finite recurrence budget below `3.0001%` of the minimum-fee first-term scale. The resulting source-shaped ceiling is `3001/100000 = 3.001%`, and the multi-term theorem derives convergence by iteration 46 rather than assuming a stop index.

The exact-LP-input caller checks `MAX_OUT_RATIO` only after `c_pow`, so the proof does not use that check as a convergence precondition. Instead, it handles a rounded remaining-supply base below `0.5` independently of the approximation: the ideal withdrawal exceeds 45% of the output-token balance after the worst configured fee, whereas a successful caller returns at most the roughly one-third maximum-output ratio. The low-base result is therefore strictly pool-favoring. Combining that result with the in-band recurrence theorem removes the public entrypoint's former `computed_base >= 0.5` restriction. `SingleSidedWithdrawalSource.lean` evaluates the checked scaling, supply subtraction, reciprocal exponent, `c_pow`, fee, downscale, and both public balance-cap guards; a successful result supplies only the recurrence prefixes actually executed and directly proves the same bound.

For the exact-LP-output deposit, both the direct supply base and reciprocal exponent are rounded upward. Lean proves that a continued approximation makes the nominal mint ratio large enough to absorb both ceiling units, bounds the rounded displacement by `1.01 * nominal_ratio`, and bounds the reciprocal fractional part by `1.001 * (1 - weight) / weight`. The precise proof splits at nominal ratio `1/200`. Below that threshold, the rounded base is below `1.01`, its reciprocal whole power is below `1.094`, and both a second stop and every later stop fit within one percent of the minimum-fee first-term scale. Bounding the composed weighted fraction by `1.096` leaves a strict `1% * 1.096 * 1.01 = 1.10696%` ceiling. At and above `1/200`, the universal 3151-unit fractional cap and the full-band whole-power bound below `68.72` are already strictly below the selected `1107/100000 = 1.107%` fee share. The later theorem derives `N <= 46`; the operation-level theorems then compose every `c_pow` control-flow case with the new-token-balance ceiling, weighted-fee division ceiling, and final token downscale ceiling.

The exact-LP-output caller enforces `MAX_IN_RATIO` only after `c_pow`, so the proof does not treat that check as a precondition. Instead, it first proves a coarse lower bound across the entire accepted above-one `c_pow` base domain: through the 50-iteration cap, the retained odd fractional partial exceeds `BONE - 3725`, the upper-rounded whole power is at least the base because the reciprocal exponent has a positive integer part, and the final ceiling preserves their product. Any rounded base above `1.6` would consequently require more than half of the input balance before the fee while the successful caller permits less than half. Thus the post-calculation guard implies `computed_base <= 1.6`, after which the sharper convergence and fee-relative bounds apply. The integer-only and corrected-first-term cases remain pool-favoring without this argument.

For direct weight exponents, the exact second term contains the same `(1 - weight)` factor as the fee. For the exact-token-output withdrawal, Lean additionally proves that all non-constant below-one terms are negative and that the entire omitted tail is at most the magnitude of the last retained term when the rounded base is at least `0.5`. Consequently, the baseline's doubled final negative term is a true lower bound after second and later stops. The one-unit first-stop correction is also conservative. Retaining the actual `1/2` contraction in the continuation estimate reduces the augmented later recurrence budget to less than `4.7501%` of the weighted minimum-fee first-term scale; the `n = 3` case is limiting, and the selected public ceiling is `4751/100000 = 4.751%`. The operation-level theorems compose those results with the before-fee ceiling, base floor, new-supply floor, and final LP-token ceiling; favorable fee-division and base-floor displacement absorbs the difference between the computed-base fee scale and the exact adjusted minimum-fee scale. The later in-band theorem derives `N <= 46` from the concrete recurrence trace.

The exact-token-output caller checks its maximum token-output ratio before `c_pow`, so the production output, weight, and fee limits bound the exact adjusted ratio by `3/8`. A pathologically small native-unit balance can nevertheless make the indivisible before-fee ceiling push the rounded computed base below `0.5`. Lean handles that region separately: the favorable gap between the exact and rounded bases is larger than the exact tail after the first term, and the concrete three-floor recurrence preserves non-positivity of every later computed term. The computed power is therefore below the exact adjusted power, making the complete LP-token result pool-favoring without requiring a low-band convergence theorem. The actual 50-iteration loop cap is an explicit trace premise in that case.

For the exact-token-input deposit, the public caller checks `MAX_IN_RATIO` before invoking `c_pow`. The fee-adjusted input floor and balance-ratio floor therefore place every computed base in `[1, 1.6]`, and the direct normalized-weight exponent lies strictly between zero and one, so no integer-power factor can amplify fractional error. Lean proves the unit-base and zero-first-term results conservative, bounds the corrected first-term result's adverse omitted tail below `1223/100000 = 1.223%` of the weighted minimum fee, and proves the second-term result pool-favoring from the signs of its three floors. For a later stop, round-down correction retains an even generalized-binomial lower partial. The public `MAX_IN_RATIO / STROOP = 0.3333334` displacement bound and the continuation threshold reduce the remaining proof to the finite cases `3 <= N <= 46`; Lean checks each permitted even retained degree against the 1.223% rate. The later theorem derives `N <= 46` from the concrete recurrence trace rather than assuming it independently. The source-shaped entrypoint theorem derives the premises for these five complete operation-level cases from the executable trace and invokes the matching theorem in each branch instead of repeating their final error composition.

Ordinary proportional `join_pool` and `exit_pool` do not call `c_pow` and do not charge a swap fee. Their relevant property is instead zero adverse rounding direction. The operation-level Lean theorems model the common LP-token upscale, each token balance's decimal upscale, the normalized ratio ceiling or floor, the token multiplication ceiling or floor, and the final token-unit downscale. Per token in the caller loop, a join therefore cannot charge less than the ideal proportional deposit and an exit cannot pay more than the ideal proportional withdrawal. `ProportionalLiquiditySource.lean` evaluates each shared ratio and every modeled token amount in list order, derives all ceiling or floor refinements from successful checked execution, includes the represented public amount-limit and balance guards, and proves the corresponding pool-favoring result for every returned token.

`InitializationSource.lean` models the successful configuration path through `init` and `execute_init`. It aligns the three public vectors, checks uniqueness and every configured range, derives each decimal scalar, models checked i128 accumulation of normalized weights, requires the exact `STROOP` total, and returns the record sequence, configured initial LP mint amount, and LP decimals. Successful execution proves vector and queried-decimal correspondence, source-order record indices, token uniqueness, every per-record bound, exact aggregate weight, valid fee, and positivity of the configured mint amount. Because `mint_shares` adds that amount to the existing persistent total, the model does not claim the resulting stored supply equals `INIT_POOL_SUPPLY`. External token metadata calls and transfers, authorization, share minting, and storage effects remain at the reviewed source-correspondence boundary.

The two public spot-price getters share the same three-floor balance-per-weight and ratio calculation; the fee-adjusted getter applies one additional floor. For active token records with at least one native balance unit, positive weights no larger than `STROOP`, and a fee below `STROOP`, Lean derives positivity of every divisor. If `D` is the positive floored output balance-per-weight value, the normalized result is strictly below `ideal + ideal / D`. Only the output-denominator floor can raise the result above the continuous ideal. In the other direction, the normalized result is strictly above the ideal less `STROOP / (exact_output_balance_per_weight * (STROOP - fee)) + 1 / (STROOP - fee) + 1 / STROOP`, accounting for the numerator, ratio, and final floors. The zero-fee final calculation returns the integer ratio unchanged; the fee-adjusted getter cannot return less than the zero-fee getter and is monotone in the fee. The same floor-refinement argument proves that the balance update performed by either swap cannot decrease its computed post-swap spot price. `SpotPriceSource.lean` defines an executable `Option` model of the successful positive-denominator sequence of `fixed_div_floor(...).unwrap_optimized()` calls in `calc_spot_price`. From the model's final `some price` result, Lean recovers the intermediate source-shaped trace automatically, uses the pinned companion i128 execution proof to derive every rounding premise, applies the complete error theorem directly to `price`, and delegates the successful fee-versus-zero-fee comparison to the corresponding operation-level theorem; no rounding or trace premise is exposed at these theorem boundaries.

A fully exited pool can have zero token balances and therefore has no defined spot-price divisor. The active-record premise is necessary rather than merely a proof convenience; getter behavior for an empty pool remains part of the contract-level API design rather than this arithmetic result.

## Fixed-point implementation refinement

The reusable checked-`I256` and checked-`i128` models are maintained on the companion `proof/formalize-i256-fixed-point` branch of `soroban-fixed-point-math`, pinned here to proof commit [`2867a65`](https://github.com/blnt-protocol/soroban-fixed-point-math/commit/2867a65e1c999f9a4242500e58aca0be22c32afa). They prove that the positive-denominator `I256` algorithms and non-widening `FixedPoint for i128` algorithms in `src/i256.rs` and `src/i128.rs` refine mathematical floor and ceiling under explicit checked-arithmetic hypotheses. That proof commit is based on `script3` upstream commit `c85960e`; its Rust source is byte-for-byte identical to the implementation shipped by the exact `soroban-fixed-point-math` 1.5.0 dependency pinned here. This project retains only the application-specific premises and operation-level compositions.

All production fixed-point denominators in `c_pow` are positive. Negative-denominator behavior is therefore outside the proof composition required by this contract.

## Assurance boundary

For `init`, the project completes the successful source-shaped composition of its configuration guards and record-building arithmetic, including checked weight accumulation, scalar derivation, token uniqueness, exact record ordering, fee bounds, exact normalized total weight, and the configured initial LP mint amount. Token-client calls, transfers, share minting, controller authorization, and storage writes are not modeled operationally.

For `swap_exact_amount_in`, `swap_exact_amount_out`, `dep_tokn_amt_in_get_lp_tokns_out`, `wdr_tokn_amt_in_get_lp_tokns_out`, `wdr_tokn_amt_out_get_lp_tokns_in`, and `dep_lp_tokn_amt_out_get_tokn_in`, this project completes the operation-level mathematical composition for every production control-flow case reached by a successful positive-base call. These results include both generalized-binomial tail directions, the three mathematical floors in every fractional recurrence update, exponentiation-by-squaring traces for upper-rounded `c_powi`, final directional `c_pow` multiplication, directional token scaling, weighted-fee multiplication or division, final integer rounding, and the exact-LP-amount paths' post-calculation ratio guards. Source-shaped execution bridges for all six entrypoints construct those traces and refinements from successful executable arithmetic-model results.

For proportional `join_pool` and `exit_pool`, the project completes the per-token operation-level mathematical composition of the production upscales, fixed-point ratio division, fixed-point token multiplication, and final token downscale. Their source bridges construct those premises for the shared ratio and every modeled token in a successful list execution, establishing the rounding direction for each full modeled loop.

For `get_spot_price` and `get_spot_price_sans_fee`, the project completes the successful positive-denominator source-shaped fixed-point composition for active token records, including checked i128 representability, divisor safety, both sides of the nested-floor error envelope, the relationship between fee-adjusted and zero-fee results, fee monotonicity, and the post-swap balance-update direction.

The project does not yet provide end-to-end machine-checked source correspondence for every deployed baseline entrypoint. Initialization, the exact-input swap, exact-output swap, all four single-sided liquidity paths, both proportional liquidity paths, and spot-price bridges now construct their modeled checked arithmetic and configuration premises from successful executable source-shaped results. As throughout the project, correspondence between the hand-written Lean models and Rust remains reviewed rather than produced by verified Rust extraction. Storage reads and writes, token transfers, share minting and burning, authorization, event emission, external token metadata calls, and public guards other than those explicitly represented in the models remain outside their scope. Primitive Soroban host operations are trusted to satisfy their protocol-specified checked-integer semantics.
